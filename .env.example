# Postgres connection string (Neon / Vercel Postgres / local).
# Local dev example: postgres://postgres:postgres@localhost:5432/value_gap
DATABASE_URL=

# Single-admin login (MVP). Generate the hash with: npm run hash-password -- 'your password'
ADMIN_EMAIL=david@planforyourgoals.com
ADMIN_NAME=David Roberts
ADMIN_PASSWORD_HASH=

# Random 32+ char secret used to sign the session cookie. `openssl rand -base64 32`
AUTH_SECRET=
